const boardInitialState = [[0,0,0],[0,0,0],[0,0,0]]

function boardReducer(state = boardInitialState, action) {
  switch (action.type) {
    /* istanbul ignore next */
    default:
      return state;
  }
}

export default boardReducer
